plugins { id "com.jfrog.bintray" version "1.8.0" }
apply plugin: 'idea'
apply from: 'publish.gradle'
apply plugin: 'groovy'

archivesBaseName = 'ida-gradle'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

