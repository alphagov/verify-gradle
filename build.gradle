apply plugin: 'idea'
apply from: 'publish.gradle'
apply plugin: 'groovy'
apply plugin: "sonar-runner"

archivesBaseName = 'ida-gradle'

dependencies {
    compile gradleApi()
    compile localGroovy()
}
