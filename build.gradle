plugins {
  id "org.sonarqube" version "2.2.1"
}

apply plugin: 'idea'
apply from: 'publish.gradle'
apply plugin: 'groovy'

archivesBaseName = 'ida-gradle'

dependencies {
    compile gradleApi()
    compile localGroovy()
}
