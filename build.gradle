apply plugin: 'idea'
apply from: 'publish.gradle'
apply plugin: 'groovy'
apply plugin: "org.sonarqube"

archivesBaseName = 'ida-gradle'

dependencies {
    compile gradleApi()
    compile localGroovy()
}
